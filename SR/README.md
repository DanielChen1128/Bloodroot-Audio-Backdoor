# Speech Recognition Pipeline

This directory contains the released SR victim-model pipeline plus reconstructed integration needed to run it publicly. See the repository [README](../README.md) for status, external assets, setup, commands, and paper-alignment limitations.

All configured relative paths are resolved against this directory. Generated data is preserved unless `--overwrite` is explicitly passed.
